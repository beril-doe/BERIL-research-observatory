{
  "name": "beril-search-island",
  "version": "0.1.0",
  "private": true,
  "description": "React island for the BERIL OpenViking search page. Built with esbuild to ui/app/static/search/search.js and mounted by templates/search.html via window.BERILSearch.mount().",
  "scripts": {
    "build": "esbuild src/index.jsx --bundle --format=esm --outfile=../../app/static/search/search.js --sourcemap --loader:.jsx=jsx --jsx=automatic",
    "watch": "npm run build -- --watch"
  },
  "dependencies": {
    "react": "19.2.5",
    "react-dom": "19.2.5"
  },
  "devDependencies": {
    "esbuild": "0.25.12"
  }
}
